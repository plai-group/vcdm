"""
A diffusion model architecture designed for embedding-type data.

Designed to be rotation-invariant (in some sense), as embeddings are (in some sense).
"""

import torch
from torch import nn



class EmbNetBlock(nn.Module):
    



class EmbNet(nn.Module):
    def __init__(self):
        self.data_channels = ...
        self.emb_channels = ...
        self.depth = ...


